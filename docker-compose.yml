services:
  db:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: envoyer
      POSTGRES_USER: envoyer
      POSTGRES_PASSWORD: envoyer
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
